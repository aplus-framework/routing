version: "3"
services:
  package:
    image: registry.gitlab.com/aplus-framework/images/package
    container_name: package-routing
    working_dir: /package
    volumes:
      - .:/package
    tty: true
